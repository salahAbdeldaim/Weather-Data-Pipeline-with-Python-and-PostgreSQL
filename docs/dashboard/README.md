# 📊 Metabase BI Dashboard Media & QR Code (`docs/dashboard/`) | المعاينة الحية وباركود لوحة البيانات

هذا المجلد يحتوي على الصور والوسائط الخاصة بلوحة ذكاء الأعمال والتحليلات التفاعلية (**`Metabase BI Dashboard`**) المستضافة على سيرفر الإنتاج السحابي الحقيقي (**`Hostinger Cloud VPS`**).

---

## 📲 باركود الوصول السريع للوحة التحليلات الحية (`Direct Scan QR Code`)

يمكن لأعضاء لجنة التقييم والمناقشة أو المقيّمين مسح هذا الباركود (`QR Code`) مباشرة عبر كاميرا الهاتف المحمول للدخول الفوري على الداشبورد الحية والتفاعل مع الرسوم البيانية لـ 260 مدينة ومحافظة مصرية:

![Live Metabase Dashboard QR Code](./dashboard.png)

👉 **[🔗 أو اضغط هنا للدخول المباشر عبر الرابط العام (`Live Public URL`)](http://72.62.92.93:3000/public/dashboard/ab63c544-c807-47da-a8c9-1490ce34f57b)**

---

## ⚙️ تفاصيل لوحة البيانات والمميزات التقنية (`Technical Features`)
- **رابط الاستضافة الحقيقي (`VPS Host`)**: `http://72.62.92.93:3000/public/dashboard/ab63c544-c807-47da-a8c9-1490ce34f57b`
- **حماية البيانات وحوكمة الوصول (`Data Governance & RBAC`)**: يتم استعراض البيانات باستخدام صلاحية القراءة فقط (`RBAC Role: team_view`) في قاعدة بيانات `PostgreSQL` لمنع أي تعديل أو حذف غير مصرح به.
- **التحديث التلقائي (`Automated Refresh`)**: تتحدث مؤشرات ومخططات اللوحة تلقائياً فور تشغيل بايبلين البايثون وجدولة `n8n` اليومية.
